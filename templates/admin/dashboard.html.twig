{% extends 'base.html.twig' %}

{% block title %}{{'adminPagetitle'|trans}}{% endblock %}

{% block body %}
    <h1>{{'adminPageTableName'|trans}}</h1>

    <ul>
        {% for user in users %}
            <li>{{ user.email }}</li>
        {% endfor %}
    </ul>
{% endblock %}
