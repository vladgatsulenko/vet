{% extends 'base.html.twig' %}

{% block title %}{{'productEditTittle'|trans}}{% endblock %}

{% block body %}
    <h1>{{'productEditBody'|trans}}</h1>

    {{ include('product/_form.html.twig', {'button_label': 'productEditUpdateButton'|trans}) }}

    <a href="{{ path('app_product_index') }}">{{'productEditList'|trans}}</a>

    {{ include('product/_delete_form.html.twig') }}
{% endblock %}
